#!/usr/bin/env sh
# managed-by: golden-path v1
# Active hook for Husky v9+ once the repo migrates from the legacy v3 setup in
# `package.json` -> `husky.hooks`. Until then, git executes `.git/hooks/pre-commit`
# (generated by husky v3) which already runs `cd react && npx lint-staged`.

cd react && npx lint-staged
