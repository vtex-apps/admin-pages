<!-- managed-by: golden-path v1 -->
# Scope of Work — `<feature-name>`

> Input to `/speckit.specify`. Keep this concise; the spec pipeline will expand it.
> Reference: <https://github.com/vtex/sdd-templates/blob/main/templates/scope_of_work/template.md>

## Context

- Why this work matters now.
- The problem in one or two paragraphs, in the language of `docs/glossary.md`.

## Outcomes

- The observable, user- or system-facing outcomes this work must deliver.
- Anti-goals (what is explicitly *not* in scope).

## Inputs

- Existing artifacts to read (RFCs, ADRs, dashboards, prior PRs).
- Domain experts to consult.

## Constraints

- Stack/architecture constraints (e.g. must run on the React builder, no new
  GraphQL schema, can't break translation equality, etc.).
- Compliance / security / data-handling constraints.
- Release / migration constraints.

## Acceptance signals

- How we will know the work is done (lint/test/coverage, manual UAT,
  observability signals).

## Open questions

- `TODO(team): list ambiguous decisions to resolve before `/speckit.specify`.`
