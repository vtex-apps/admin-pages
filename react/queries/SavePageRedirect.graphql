mutation SavePageRedirect(
  $active: Boolean
  $endDate: String
  $fromUrl: String
  $toUrl: String
) {
  savePageRedirect(
    active: $active
    endDate: $endDate
    fromUrl: $fromUrl
    toUrl: $toUrl
  )
}
