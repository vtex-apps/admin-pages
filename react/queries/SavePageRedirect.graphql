mutation SavePageRedirect(
  $fromUrl: String,
  $toUrl: String,
  $endDate: String,
  $active: Boolean
  ) {
  savePageRedirect(
    fromUrl: $fromUrl
    toUrl: $toUrl
    endDate: $endDate
    active: $active
  )
}
