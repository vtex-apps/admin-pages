query Conditions($locale: String, $page: String, $renderMajor: Int) {
  page(
    locale: $locale,
    page: $page,
    params: "{}",
    production: false,
    query: "{}",
    renderMajor: $renderMajor,
  ) {
    conditionsJSON,
  }
}
