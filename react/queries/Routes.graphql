query Routes {
  routes {
    id
    declarer
    path
    cname
    context
    pages {
      declarer
      name
      template
      device
      conditions
      allMatches
      configurationId
    }
  }
}
