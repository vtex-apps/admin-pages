query Routes {
  routes {
    id
    declarer
    path
    cname
    context
    login
    pages {
      declarer
      name
      template
      device
      conditions
      allMatches
      configurationId
    }
  }
}
