query Routes($domain: String) {
  routes(domain: $domain) {
  auth
  blockId
  context
  declarer
  domain
  interfaceId
  path
  routeId
  uuid
  pages {
    pageId
    conditions
    template
  }
  title
}
}
