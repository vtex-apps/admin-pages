mutation saveRedirect(
  $id: ID
  $endDate: String!
  $from: String!
  $to: String!
  $type: String!
) {
  saveRedirect(id: $id, endDate: $endDate, from: $from, to: $to, type: $type) {
    cacheId
    disabled
    endDate
    from
    id
    to
    type
  }
}
