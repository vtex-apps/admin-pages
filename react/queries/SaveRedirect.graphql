mutation saveRedirect(
  $endDate: String!
  $from: String!
  $to: String!
  $type: String!
) {
  saveRedirect(endDate: $endDate, from: $from, to: $to, type: $type) {
    cacheId
    disabled
    endDate
    from
    id
    to
    type
  }
}
