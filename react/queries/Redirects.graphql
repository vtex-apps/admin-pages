query Redirects($from: Int!, $to: Int!) {
  redirect @context(provider: "vtex.rewriter") {
    list(from: $from, to: $to) {
      endDate
      from
      to
      type
    }
    numberOfEntries
  }
}
