query Redirect($locator: RouteLocator!) {
  redirect @context(provider: "vtex.rewriter") {
    get(path: "", locator: $locator) {
      endDate
      from
      to
      type
      binding
    }
  }
}
