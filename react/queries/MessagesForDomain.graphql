query GetMessagesForDomain(
  $components: [String!]!
  $domain: String!
  $renderMajor: Int!
) {
  messages(
    components: $components
    domain: $domain
    renderMajor: $renderMajor
  ) {
    key
    message
  }
}
