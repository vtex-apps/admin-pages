query Route(
  $domain: String
  $routeId: String!
) {
  route(
    domain: $domain
    routeId: $routeId
  ) {
    auth
    blockId
    context
    declarer
    domain
    interfaceId
    path
    routeId
    uuid
    pages {
      pageId
      conditions
      template
    }
    title
  }
}
