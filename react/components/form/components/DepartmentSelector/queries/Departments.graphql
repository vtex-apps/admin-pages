query Departments($query: String = "") {
  departmentSearch(query: $query) {
    cacheId
    href
    slug
    id
    name
    titleTag
    hasChildren
    metaTagDescription
    children {
      cacheId
      href
      slug
      id
      name
      titleTag
      hasChildren
      metaTagDescription
    }
  }
}
