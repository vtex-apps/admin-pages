query Categories($query: String = "") {
  categorySearch(query: $query) {
    cacheId
    href
    slug
    id
    name
    titleTag
    hasChildren
    metaTagDescription
    children {
      cacheId
      href
      slug
      id
      name
      titleTag
      hasChildren
      metaTagDescription
    }
  }
}
