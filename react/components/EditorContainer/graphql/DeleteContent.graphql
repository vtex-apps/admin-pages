mutation DeleteContent(
  $template: String
  $treePath: String
  $pageContext: PageContextInput
  $contentId: String
) {
  deleteContent(
    template: $template
    treePath: $treePath
    pageContext: $pageContext
    contentId: $contentId
  )
}
