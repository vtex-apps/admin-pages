import React from 'react'

interface Props {
  date: undefined
  onChange: (event: React.ChangeEvent<HTMLInputElement>) => void
}

const DateSelector: React.SFC<Props> = ({ onChange }) => {
  return <div />
}

export default DateSelector
